jobs:cloud drive and sync provider
  publish: /HOME 
    runs-on: ${{ elparadisogonzalo.os }}
    strategy:"full-stackLLM
      matrix:
        os: [elparadisogonzalo.os]
    steps:
      - run: echo "Running on ${{elparadisogonzalo.os }}"
